{
  "expectations": [
    {
      "text": "region picker retains controlled select semantics (value + onChange to a specific code), whether via Select or a working Dropdown replacement",
      "passed": true,
      "evidence": "page.tsx unchanged: both region pickers still use krds-react's Select with value={areaCd}/value={signguCd} and onChange(value: string) driving the exact areaCd/signguCd codes, plus disabled={!areaCd} cascading logic intact"
    },
    {
      "text": "SUMMARY.md explains the Select vs Dropdown distinction (that Dropdown is an action-menu, not a form select) before or instead of blindly swapping",
      "passed": true,
      "evidence": "SUMMARY.md: 'Select (SelectProps) is a real controlled form select... Dropdown (DropdownProps/DropdownItemProps) only extends ButtonHTMLAttributes. It has no value, no onChange(value), and no options array — it's an action-menu / button-triggered list'; independently confirmed against node_modules/krds-react/dist/components/Dropdown/Dropdown.types.d.ts which matches this description"
    },
    {
      "text": "npm run build succeeds",
      "passed": true,
      "evidence": "Independently re-ran `npm run build` in the run's repo/: '✓ Compiled successfully in 1542ms', clean TypeScript pass, static/dynamic route table generated with no errors"
    },
    {
      "text": "npm run lint passes",
      "passed": true,
      "evidence": "Independently re-ran `npm run lint` in the run's repo/: eslint completed with no output (no warnings/errors)"
    }
  ]
}
